<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodrecipe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClientEdit"></activity>
        <activity android:name=".ClientProfileActivity" />
        <activity android:name=".RequestList" />
        <activity android:name=".RequestActivity" />
        <activity android:name=".ClientSideRecipeActivity" />
        <activity android:name=".RecipeProfileActivity" />
        <activity android:name=".RecipeActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>